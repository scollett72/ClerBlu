trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- checkout: self

- task: NodeTool@0
  inputs:
    versionSpec: '14.x'
  displayName: 'Install Node.js'

- script: cd Playwright && npx playwright install
  displayName: 'Install Playwright browsers'

- script: cd Playwright && npm install
  displayName: 'Install Dependencies'
  
- script: cd Playwright && npm test
  displayName: 'Run Playwright tests'
